name: Email Token
description: Generates token for current site url and current node being access. Also provide token for email functionality.
package: Token

type: module
core: 8.x
version: '8.x-1.0'
